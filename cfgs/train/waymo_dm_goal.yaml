defaults:
- base

run_name: scenario_dreamer_dm_goal_base_waymo
lr_schedule: constant
save_top_k: 0
max_steps: 165000
warmup_steps: 500
weight_decay: 1e-5
check_val_every_n_epoch: 2
ema_decay: 0.9999
guidance_scale: 4.0
devices: 4
num_samples_to_visualize: 10
viz_dir: ${project_root}/viz_val_${dm_goal.train.run_name}

lane_weight: 10
agent_type_weight: 1.0
lane_conn_weight: 10
